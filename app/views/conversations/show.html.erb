<div class="login-sub-nav"></div>
<h1 style="text-align: center">Chatting with <%= @conversation.with(current_user).name %></h1>

<div id="conversation-main">
  <div id="conversation-body">
    <%= render @conversation.personal_messages %>
  </div>
  <%= form_for @personal_message do |f| %>
    <div class="container">
      <%= hidden_field_tag 'conversation_id', @conversation.id %>
      <%= f.text_area :body, class: 'conversation-input' %>
    </div>
    <div class="call-to-action bottom">
      <%= f.submit 'Send message', class: 'btn btn-flat' %>
    </div>
  <% end %>
</div>
<div class="sub-footer"></div>
